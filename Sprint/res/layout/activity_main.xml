<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/scanButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="25dp"
        android:background="#888888"
        android:paddingTop="5dp"
        android:drawableTop="@drawable/qrbypdp"
        android:onClick="startScanner" />

    <Button
        android:id="@+id/userButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/printerButton"
        android:layout_below="@+id/printerButton"
        android:layout_marginTop="25dp"
        android:background="#888888"
        android:paddingTop="5dp"
        android:onClick="startPreferences"
        android:drawableTop="@drawable/preferences" />

    <Button
        android:id="@+id/printerButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/scanButton"
        android:layout_marginTop="25dp"
        android:background="#888888"
        android:drawableTop="@drawable/select_printer"
        android:onClick="startPrinterListActivity"
        android:paddingTop="5dp" />

</RelativeLayout>